# build-something — 작업 지침 (DIRECTIVE)

이 폴더는 "그냥 만들고 노는" 실험장이다. 두 가지 작업 모드를 **번갈아가며**
매 턴 진행한다. 둘 다 `/ponytail full` 모드로 작성한다 (최소 코드, 무의미한
추상화 금지, 자체 검증 필수).

## 모드 A — 자유 빌드 (free build)
- 아무 프로젝트나 막 만들기. 새 폴더 하나 = 독립 실행 가능한 작은 도구/장난감.
- 조건:
  - 실행 가능해야 한다 (dotnet run / node / python / go / cargo / 브라우저).
  - 비결정적 로직에는 `--demo` 또는 assert 자체 검증을 남긴다.
  - **create 금지 없음** — 웹게임 포함, 완전한 창의적 자유. "재밌음 / 디벨롭
    여지 / 의외의 강력한 조합" 관점이 중요. "쓸만함"만 보는 게 아님.
  - 언어는 골고루: Python 편중 지양. 필요한 툴체인(Rust/Go 등)은 용량이 크지
    않다면 알아서 설치해서 쓴다.
  - `README.md` 표에 추가.

## 모드 B — 3개 연쇄 빌드 (chain build)
- 기존 프로젝트 폴더 중 **무작위 3개를 고른다 (중복 허용)**.
- 그 3개를 자연스럽게 이어지는 **input → process → output** 파이프라인으로
  엮는 **새 프로젝트** 하나를 만든다.
  - 한 도구의 출력이 다음 도구의 입력이 되도록 한다 (stdout→stdin, 또는
    오케스트레이터가 출력을 다음 도구의 인자/파일로 넘김).
  - 억지로 이을 필요는 없다 — "어떻게든 자연스럽게"가 핵심.
- 새 프로젝트는 보통 오케스트레이터(C# 콘솔 또는 bash/node/python) 형태. 하위
  도구는 그대로 재사용 (재구현 금지).
- `README.md` 표에 추가.

## Git / GitHub 워크플로우 (기본 운영 규칙)
- **매 턴 끝에 변경사항은 알아서 커밋 + 푸시**한다 (누락 금지).
- **작고 명확한 작업**은 main(또는 현재 작업 브랜치)에 바로 커밋·푸시해도 됨.
- **각잡은 작업**(새 언어 툴체인 세팅, 큰 기능 추가, 리팩터, 여러 파일 영향,
  사용자가 "구체적/각잡은" 뉘앙스를 보인 경우)은 아래 흐름을 따른다:
  1. `gh issue create` 로 이슈 생성 (목표/수용조건 명시)
  2. `git checkout -b feat/...` 브랜치 파생
  3. 작업 + 커밋 + `git push -u origin <branch>`
  4. `gh pr create` 로 PR 생성
  5. `gh pr review` (또는 직접 diff 점검) 로 리뷰
  6. 이상 없으면 `gh pr merge` 로 머지 (main 으로)
- 빌드 산출물(*.exe, target/, melody.wav 등)은 `.gitignore` 로 제외.
- 사용자가 interrupt 하지 않는 한 계속 돌린다. 매 턴 끝에 만든 것 1~2줄 요약.

## 규칙
- 사용자에게 "뭘 원하냐" 묻지 않는다. 그냥 계속 생산한다.
